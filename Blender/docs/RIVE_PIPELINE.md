# Rive Pipeline

Expected state machine: `CodexAssistant` with inputs `state`, `cursorX`, `cursorY`, `mouthOpen`, `scrollProgress`, `isSpeaking`, `isThinking`, and triggers `wave`, `celebrate`, `confused`, `point`. Fall back to SVG if `.riv` is missing.
